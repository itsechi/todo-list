export default class Todo {
  constructor(title, description, dueDate, project, priority, status) {
    this.title = title;
    this.description = description;
    this.dueDate = dueDate;
    this.project = project;
    this.priority = priority;
    this.status = status;
  }
}

